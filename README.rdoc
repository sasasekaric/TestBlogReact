== README

== Instalation

git clone url
cd test_blog
cp ./.env.example ./.env
cp ./config/database.yml.example ./config/database.yml
# Set username and password inside config/database.yml for your database
bin/setup
sudo service cron start
rails s
