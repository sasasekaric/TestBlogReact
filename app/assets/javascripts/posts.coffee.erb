class Post
  constructor: () ->
    # $('.card').click @hadlePostClick

  # hadlePostClick: (e) ->
  #   postId = e.currentTarget.dataset.post
  #   $.ajax
  #     url: '/posts/' + postId
  #     dataType: 'script'
  #   .success () ->

$ ->
  new Post
